---
- case: record_true_no_error
  parametrized:
    - severity: info
    - severity: debug
    - severity: error
    - severity: trace
  main: |
    from loguru import logger

    LOG = logger.opt(record=True)

    LOG.{{ severity }}('a')
    LOG.{{ severity }}('r={record["line"]}')
